// Testing whether C++ programs work in the same VSCode project as the Python ones
// To compile .cpp from outside:
// g++ {file_name}.cpp -o {output_name}

#include <iostream>

int main() {
    std::cout << "Working!";
    return 0;
}
